{
  "name": "allerstamp-backend",
  "version": "1.0.0",
  "description": "AllerStamp backend — allergen profiles, label matching, and AI verdict explanations",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "sync-frontend": "mkdir -p public && cp -r ../frontend/public/. ./public/"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "uuid": "^9.0.1"
  }
}
